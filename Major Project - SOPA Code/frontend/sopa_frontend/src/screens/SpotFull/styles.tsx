import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  center: {
    textAlign: "center",
    padding: 20,
    fontSize: 30,
    marginVertical: 20,
  },
});

export default styles;
